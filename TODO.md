
## osx settings
  - filco minila
    - remap alt to super
    - remap super to alt
    - set key repeat and delay time
    - add US input source
  - japanese input source
  - disable auto-hide on dock
  - default to firefox browser


## setup python
  - pyenv install 3.6.5 (latest 3.6)
  - pyenv install 2.7.15 (latest 2.7)
  - pyenv global 3.6.5
  - pyenv rehash
  - reload env (source ~/.zshrc)
  - pip install --upgrade pip

## setup ruby
  - rbenv install 2.5.1 (latest 2.5)
  - rbenv global 2.5.1
  - rbenv rehash
  - reload env (source ~/.zshrc)
  - gem install bundler pry

## setup zsh
  - change shell for user to zsh
  - oh-my-zsh install
  - rm ~/.vimrc when complete
  - stow ~/.dotfiles/zsh

## setup vim
  - mkdir -p ~/.vim/colors
  - vundle install
  - stow ~/.dotfiles/vim
  - install powerline-status

## setup u
  - download u
  - u config
 
